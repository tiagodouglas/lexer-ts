export enum TokenType {
    ILLEGAL = "ILLEGAL",
    IDENT = "IDENT",
    EOF = "EOF",
    INT = "INT",
    ASSIGN = "=",
    PLUS = "+",
    MINUS = "-",
    BANG = "!",
    ASTERISK = "*",
    SLASH = "/",
    LT = "<",
    GT = ">",
    EQ = "==",
    NOT_EQ = "!=",
    COMMA = ",",
    SEMICOLON = ";",
    LPAREN = "(",
    RPAREN = ")",
    LBRACE = "{",
    RBRACE = "}",
    FUNCTION = "FUNCTION",
    LET = "LET",
    FALSE = "FALSE",
    TRUE = "TRUE",
    IF = "IF",
    ELSE = "ELSE",
    RETURN = "RETURN"
  };